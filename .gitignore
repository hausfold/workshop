# The family repos live inside this dir but are their own git repos.
#
# ✅ §3.3's checklist has run, so these two now mean what §3.1 decided:
# `hausfold/` is the PLATFORM checkout (`hausfold/hausfold` — this was
# `nebelhaus/` until 2026-08-09) and `hausfold.co/` is the SITE
# (`hausfold/hausfold.co`, not in FAMILY). Two different repos; each directory
# is named for its own. `/nebelhaus/` is gone from this list because no such
# checkout exists any more — if one ever reappears, something re-cloned the
# rice under its pre-migration name and `bench` will not find it.
/hausfold/
/hausfold.co/
/holt/
/nebelung/
/pounce/
/perch/
/org-profile/
/homebrew-tap/
/web/.wrangler/
.DS_Store

# Coverage artifacts from `xcodebuild test` in the incubator projects.
*.profraw
