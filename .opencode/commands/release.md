---
description: Cut a release — pick the version, tag it, watch CI publish, ripple the lock.
---

Read `.agents/skills/release/SKILL.md` and follow it exactly, start to finish. It
is the shared release flow every client in this repo runs; do not improvise a
shorter version of it, and do not run `bench release` until I say go.

Extra context for this run (may be empty): $ARGUMENTS
