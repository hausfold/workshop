{
  "name": "nebelhaus-web",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "description": "nebelhaus.com — the 301 map to hausfold.co, and nothing else",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "vitest": "^3.2.4",
    "wrangler": "^4.40.0"
  }
}
